<h1>Add Patient</h1>
<%# form_with(model: @course, local: true, class: 'ui form') do |f| %>

<%= form_for @patient do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, placeholder: 'Patient name' %>

  <%= f.submit nil, class: 'ui blue button' %>
<% end %>